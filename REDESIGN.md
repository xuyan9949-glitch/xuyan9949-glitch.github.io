# XXY Alpha 投资研究博客改版

2026-09-12，依据“打开网页规律”讨论中的最终方向制作。工作副本位于本目录，已合并线上 5dfd6fc 的最新内容，保留交易追踪数据。此版本包含获用户授权发布的长文阅读与下载功能。

## 查看

本机预览：[打开首页](http://127.0.0.1:8092/)。如服务未启动，在本目录运行：

```sh
python3 -m http.server 8092 --bind 127.0.0.1
```

## 这版包含

- 首页：暖白、深绿、阅读优先。最近文章、重点事件、公司档案与长期学习入口。
- 事件档案：3 个基于既有文章的档案，美光财报是首个完整的文章关联样本；原文章顺序阅读、关联公司、背景经验、材料缺口。
- 研究档案：MU、NVDA、AVGO、LITE、COHR、ORCL、TSLA 七个空间；按时间或研究问题查看。TSLA 暂无文章，明确保留空状态。
- 学习与经验：三条讨论中的观察，保留假说、边界、反例与版本；与已有学习文章分开。
- 日历：历史日期、原预期与待验证问题；默认本月，无内容时明确提示；旧资料不冒充未来提醒。
- 全站搜索：标题、摘要、公司、标签、关键词；类型筛选与时间排序。
- 45 篇旧文：正文原样保留，统一阅读外观，加入公司及事件上下文；原发布日期、摘要、标签和公开/归档/回收站状态不变。
- 长文阅读：章节目录、前后章节、阅读进度、字号、专注模式、同浏览器继续阅读提示。
- 离线下载：45 篇文章各有真实 PDF / DOCX，带原文链接、日期、页码，PDF 支持章节书签；原文指纹防止过期文件继续展示。
- 记录工作台：三问记录、事件日期、实际保存时间、记录性质、公司与事件关联、追加修订、浏览器持久保存、JSON 备份导出与去重恢复。

## 必须清楚的边界

文章标注日期不等于已经证明的首次记录时间。目前时间线是按文章日期逐篇阅读，不是严格的历史信息回放。未补写个人当时的判断、买卖、仓位、连续行情或事件因果。新增导读与假说标注为本次整理。

工作台只是本浏览器的草稿本，不是云端管理后台。保存不会更新公开网站，不会跨设备同步。公开内容继续经既有导入/发布流程发布。清理浏览器存储前须导出备份。

日历没有自动获取新事件或发送通知。连续行情、机构附件和原始资料仍需进一步收集。草稿可以存来源链接，暂不上传附件。

## 验证

- `node --check js/journal.js`、`node --check js/archive-data.js`。
- `tools/test_journal.cjs` 用 jsdom 验证每条改版路线的 DOM 渲染、内部链接、原文完整性、搜索/分类/排序、逐篇阅读、公司归档、历史日历、草稿恢复与追加修订、异常存储保护。
- 本次共通过 79 项 DOM、数据与行为检查。
- 视觉浏览器测试被浏览器工具的安全检查不可用阻断；未绕过。桌面/手机实际渲染仍待验收，未将 DOM 测试描述为视觉验收。
- 未修改原桌面工作目录或交易追踪工具数据。
- 通过阅读工具栏测试、45 篇原文指纹与 90 个文件检查、DOCX 正文逐段核对和 PDF 中文文本检查；导出文件经 LibreOffice 渲染逐页检查。
- 原有美光机构分析文章缺少一张归档图，网页和离线版均明确标注原图暂缺。

测试依赖仅用于开发，不属于线上网站：

```sh
npm install --prefix /tmp/xxyalpha-qa jsdom@26 --no-audit --no-fund
NODE_PATH=/tmp/xxyalpha-qa/node_modules node tools/test_journal.cjs
```

## 后续维护

文章索引继续使用 `js/articles.js`，文章状态继续使用 `content/articles.json`。档案关系的唯一维护入口是 `content/archives.json`；`js/archive-data.js` 是生成物，请勿单独手改。

```sh
python3 tools/sync_journal.py
```

该命令同步档案数据、缺少的档案详情页面、文章阅读外壳、页面地址与站点地图。现有导入工具末尾已自动调用它；从 Markdown 导入新文章后，公司代码及财报标签自动参与检索。事件与经验的关联需要在 `archives.json` 中明确登记，避免自动推断因果。

新增公司须填写 `id`、`name`、`sector`、`question`、`ids`；`ids` 用于补充没有公司标签的关联文章。公司暂停研究后应保留其档案，不删除历史。

事件必须填写 `id`、`title`、`topic`、`period`、`status`、`description`、`companies`、`articleIds`、`question`、`gaps`。这里的 `status` 是资料整理状态，不自动推断行情是否结束。

经验须填写 `id`、`title`、`observation`、`boundary`、`counter`、`question`、`articleIds`、`eventIds`。本版本默认所有新增经验仍处于观察中；若未来开放状态变更，须同步加入证据记录和修订历史。

发布前应核对远端最新版本，保留其他任务对交易追踪等子站的修改；不能用这份较早的工作副本覆盖整个远端仓库。合并应仅包含本次改版文件，常规发布需完成桌面与手机视觉验收。本次浏览器工具安全检查不可用，已向用户披露；按用户上线授权执行，并单独核对托管发布状态。
